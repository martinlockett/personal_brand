**Rule** — a fine horizontal divider. The brand favours hairlines over heavy blocks.

```jsx
<Rule />
<Rule variant="brass" />
```

Variants: `paper` (default, faint), `strong`, `brass` (the signature hairline — use once per layout to separate the wordmark or mark a key boundary). Pass `inset` to pull it in from the edges within a card.
